Testing go module versioning, with particular respect to:
- What happens if the go entrypoint is in a subdirectory of the repo
- What happens with non-standard prefixes (namely tags with the prefix go-vX.X.X)
